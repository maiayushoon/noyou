# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data / ORM
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Auth & security
bcrypt==4.2.1
pyjwt==2.10.1
python-multipart==0.0.20

# Background jobs
apscheduler==3.11.0

# Rate limiting + production server
slowapi==0.1.9
gunicorn==23.0.0

# Billing + DB migrations
stripe==11.4.0
alembic==1.18.4

# HTTP client (real connectors / LLM providers)
httpx==0.28.1

# Machine learning — the trainable, offline reputation model (no GPU, no API keys).
# TF-IDF + linear classifier; retrain with `python -m app.ml.train`.
scikit-learn==1.5.2
joblib==1.4.2

# Config
python-dotenv==1.0.1

# Postgres driver (optional — only needed if DATABASE_URL points at Postgres)
psycopg2-binary==2.9.10

# Testing
pytest==8.3.4
