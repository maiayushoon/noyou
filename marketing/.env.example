# The canonical, fully-qualified URL of the deployed marketing site.
# Used by metadataBase, sitemap.xml, robots.txt, JSON-LD, and Open Graph tags.
# No trailing slash. Defaults to https://noyou.app when unset.
NEXT_PUBLIC_SITE_URL=https://noyou.app
